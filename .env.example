# OpenAI API Configuration
# Required for vf-eval and the smoke target
OPENAI_API_KEY=your_openai_api_key_here

# Optional defaults
# If set, these override Make's defaults for `make smoke` and also serve as
# defaults for the optional programmatic runner (`main.py`).
OPENAI_MODEL=gpt-4o-mini          # default model name
NUM_EXAMPLES=1                    # default for -n
ROLLOUTS_PER_EXAMPLE=1            # default for -r
